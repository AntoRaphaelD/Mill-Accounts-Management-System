# TODO - Global font improvements (Accessibility)

- [x] Inspect existing global styling (tailwind + index.css) and decide best single change point.
- [x] Update global CSS to slightly increase base font size and switch default text to dark color.
- [ ] Verify print styles remain readable; adjust print color if needed.
- [ ] Ensure all frontend screens use the updated global styles (no component-level overrides missed).
- [ ] Run frontend build / lint (if available) to confirm no CSS issues.


